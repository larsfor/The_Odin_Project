<%= form_with(model: projectform) do |form| %>
  <% if projectform.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(projectform.errors.count, "error") %> prohibited this projectform from being saved:</h2>

      <ul>
        <% projectform.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
